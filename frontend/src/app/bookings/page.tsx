import BookingWizard from '../../components/BookingWizard';

export default function BookingPage() {
  return (
    <main style={{ minHeight: '100vh', background: '#191919', padding: 32 }}>
      <BookingWizard />
    </main>
  );
}
